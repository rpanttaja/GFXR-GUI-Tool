<Window x:Class="GFXRTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:GFXRTool.ViewModels"
        Title="GFXR Capture Tool"
        Width="960" Height="660"
        MinWidth="720" MinHeight="520"
        Background="{StaticResource BgBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>   <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Main content -->
            <RowDefinition Height="Auto"/> <!-- Launch bar -->
            <RowDefinition Height="28"/>   <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- ── Header ─────────────────────────────────────────────────── -->
        <Border Grid.Row="0" Background="{StaticResource AccentBrush}">
            <Grid Margin="16,0">
                <TextBlock Text="GFXR Capture Tool"
                           FontSize="15" FontWeight="SemiBold"
                           Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Text="Graphics Frame Recorder  |  Standard · Injection · System32"
                           FontSize="11" Foreground="#B3DEFF"
                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ── Main content ───────────────────────────────────────────── -->
        <TabControl Grid.Row="1">

            <!-- Tab 0: Capture setup -->
            <TabItem Header="Capture">
                <Grid Margin="12,10,12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="340"/>
                    </Grid.ColumnDefinitions>

                    <!-- Games panel -->
                    <GroupBox Grid.Column="0" Header="Game Library">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ListView Grid.Row="0"
                                      ItemsSource="{Binding Games}"
                                      SelectedItem="{Binding SelectedGame}">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Game Name" Width="260">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}"
                                                               TextTrimming="CharacterEllipsis"
                                                               ToolTip="{Binding ExecutablePath}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Source" DisplayMemberBinding="{Binding Source}" Width="70"/>
                                        <GridViewColumn Header="Executable" Width="240">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding ExecutablePath}"
                                                               Foreground="{StaticResource DimTextBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               ToolTip="{Binding ExecutablePath}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                            <!-- Scan progress -->
                            <ProgressBar Grid.Row="2"
                                         IsIndeterminate="True" Height="3"
                                         Foreground="{StaticResource AccentBrush}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Visibility="{Binding IsScanning,
                                             Converter={StaticResource BoolToVis}}"/>

                            <!-- Buttons -->
                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Scan Games"
                                        Style="{StaticResource PrimaryBtn}"
                                        Command="{Binding ScanGamesCommand}"
                                        Margin="0,0,6,0"/>
                                <Button Content="Add Manually..."
                                        Style="{StaticResource SecondaryBtn}"
                                        Command="{Binding AddGameManuallyCommand}"
                                        Margin="0,0,6,0"/>
                                <Button Content="Remove"
                                        Style="{StaticResource SecondaryBtn}"
                                        Command="{Binding RemoveGameCommand}"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>

                    <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                                  Background="{StaticResource BorderBrush}"/>

                    <!-- Right panel: deployment mode + capture settings -->
                    <GroupBox Grid.Column="2" Header="Deployment Mode">
                        <StackPanel>

                            <!-- Standard Deployment -->
                            <Border Background="#1A2A1A" BorderBrush="#2D5A2D"
                                    BorderThickness="1" CornerRadius="4"
                                    Padding="10,8" Margin="0,0,0,8">
                                <StackPanel>
                                    <RadioButton Content="Standard Deployment"
                                                 IsChecked="{Binding UseStandard}"
                                                 GroupName="LaunchMethod"
                                                 FontWeight="SemiBold" FontSize="12"/>
                                    <TextBlock Text="Copies GFXR layers into the game folder before launch and removes them automatically when the game exits. No system files are modified."
                                               Foreground="{StaticResource DimTextBrush}"
                                               FontSize="11" TextWrapping="Wrap"
                                               Margin="18,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Injection -->
                            <Border BorderThickness="1" CornerRadius="4" Padding="10,8" Margin="0,0,0,8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1E1E1E"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UseInjection}" Value="True">
                                                <Setter Property="Background" Value="#0F1A28"/>
                                                <Setter Property="BorderBrush" Value="#2E7D32"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <RadioButton Content="Injection"
                                                 IsChecked="{Binding UseInjection}"
                                                 GroupName="LaunchMethod"
                                                 FontWeight="SemiBold" FontSize="12"/>
                                    <TextBlock Text="Stages GFXR layers in the game folder, then launches the game through Steam or Epic so EAC / BattlEye sees an officially-trusted process. Layers are removed automatically on exit. Requires a Steam or Epic AppId."
                                               Foreground="{StaticResource DimTextBrush}"
                                               FontSize="11" TextWrapping="Wrap"
                                               Margin="18,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- System32 Deployment -->
                            <Border BorderThickness="1" CornerRadius="4" Padding="10,8" Margin="0,0,0,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1E1E1E"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UseSystem32}" Value="True">
                                                <Setter Property="Background" Value="#2A1A0A"/>
                                                <Setter Property="BorderBrush" Value="#8B4513"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <RadioButton Content="System32 Deployment"
                                                 IsChecked="{Binding UseSystem32}"
                                                 GroupName="LaunchMethod"
                                                 FontWeight="SemiBold" FontSize="12"
                                                 Foreground="#F4A460"/>
                                    <TextBlock Text="Replaces d3d11.dll, d3d12.dll and dxgi.dll in System32. Use when Standard Deployment fails."
                                               Foreground="{StaticResource DimTextBrush}"
                                               FontSize="11" TextWrapping="Wrap"
                                               Margin="18,4,0,0"/>

                                    <!-- System32 details — only when selected -->
                                    <StackPanel Visibility="{Binding UseSystem32, Converter={StaticResource BoolToVis}}"
                                                Margin="0,10,0,0">
                                        <TextBlock Text="WARNING: If the device shuts off during capture it may brick and require a reflash. Originals are backed up as *_ms.dll and auto-restored when the game exits."
                                                   Foreground="#CE9178" FontSize="11"
                                                   TextWrapping="Wrap" Margin="0,0,0,8"/>

                                        <Border Background="#1E1E1E" BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1" CornerRadius="3"
                                                Padding="8,4" Margin="0,0,0,8"
                                                HorizontalAlignment="Left">
                                            <TextBlock FontSize="11" FontWeight="SemiBold"
                                                       Text="{Binding System32StatusText}"
                                                       Foreground="{Binding System32StatusColor}"/>
                                        </Border>

                                        <Button Content="Check Status"
                                                Style="{StaticResource SecondaryBtn}"
                                                Command="{Binding RefreshSystem32StatusCommand}"
                                                HorizontalAlignment="Stretch" Margin="0,0,0,4"/>
                                        <Button Content="Install DLLs"
                                                Style="{StaticResource SecondaryBtn}"
                                                Command="{Binding InstallToSystem32Command}"
                                                IsEnabled="{Binding IsNotBusy}"
                                                HorizontalAlignment="Stretch" Margin="0,0,0,4"/>
                                        <Button Content="Restore Originals"
                                                Style="{StaticResource SecondaryBtn}"
                                                Command="{Binding RestoreSystem32Command}"
                                                IsEnabled="{Binding IsNotBusy}"
                                                HorizontalAlignment="Stretch" Margin="0,0,0,4"/>
                                        <Button Content="Force Restore (SFC/DISM)"
                                                Style="{StaticResource SecondaryBtn}"
                                                Command="{Binding ForceRestoreSystem32Command}"
                                                IsEnabled="{Binding IsNotBusy}"
                                                HorizontalAlignment="Stretch"
                                                Foreground="#F44747"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- ── Capture settings ──────────────────────────── -->
                            <Separator Margin="0,12,0,10" Background="{StaticResource BorderBrush}"/>

                            <TextBlock Text="Capture Output Directory"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource DimTextBrush}"
                                       Margin="0,0,0,5"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0" Height="26">
                                    <TextBox Text="{Binding CaptureOutputDir, UpdateSourceTrigger=PropertyChanged}"
                                             VerticalContentAlignment="Center"/>
                                    <TextBlock Text="default_gfxr_location  (next to exe)"
                                               Foreground="{StaticResource DimTextBrush}"
                                               IsHitTestVisible="False"
                                               VerticalAlignment="Center"
                                               Margin="6,0,0,0"
                                               Opacity="0.6">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding CaptureOutputDir}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding CaptureOutputDir}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                                <Button Grid.Column="2"
                                        Content="Browse..."
                                        Style="{StaticResource SecondaryBtn}"
                                        Command="{Binding SelectCaptureOutputDirCommand}"
                                        Padding="8,4"/>
                            </Grid>

                            <!-- Set GFXR Trigger section -->
                            <Separator Margin="0,12,0,10" Background="{StaticResource BorderBrush}"/>

                            <TextBlock Text="GFXR Trigger"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource DimTextBrush}"
                                       Margin="0,0,0,8"/>

                            <!-- Env var status pill -->
                            <Border CornerRadius="3" Padding="8,4" Margin="0,0,0,8"
                                    HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1E1E1E"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="{Binding EnvStatusColor}"/>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding EnvStatusText}"
                                           Foreground="{Binding EnvStatusColor}"
                                           FontSize="10" TextWrapping="Wrap" MaxWidth="280"/>
                            </Border>

                            <!-- Trigger key + Use Trigger checkbox -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Trigger key:"
                                           FontSize="11" Foreground="{StaticResource DimTextBrush}"
                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <ComboBox ItemsSource="{Binding AvailableTriggerKeys}"
                                          SelectedItem="{Binding TriggerKey}"
                                          Width="72"/>
                                <CheckBox Content="Show overlay"
                                          IsChecked="{Binding UseTrigger}"
                                          VerticalAlignment="Center"
                                          Margin="16,0,0,0"
                                          FontSize="11"
                                          ToolTip="Show the trigger overlay while the game runs"/>
                            </StackPanel>

                            <!-- Apply button -->
                            <Button Content="Apply Trigger Settings"
                                    Style="{StaticResource SecondaryBtn}"
                                    Command="{Binding ApplyGfxrEnvVarsCommand}"
                                    HorizontalAlignment="Left"
                                    Padding="10,5"
                                    ToolTip="Saves GFXRECON_CAPTURE_TRIGGER and GFXRECON_CAPTURE_FILE_DIR as User environment variables. A system restart is required for the settings to take effect."/>

                        </StackPanel>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Tab 1: Replay -->
            <TabItem Header="Replay">
                <Grid Margin="12,10,12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: capture file + replay exe -->
                    <GroupBox Grid.Column="0" Header="Capture File">
                        <StackPanel>

                            <TextBlock Text="GFXR Capture (.gfxr)"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource DimTextBrush}"
                                       Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding Replay.CaptureFilePath, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalContentAlignment="Center"
                                         Height="26"/>
                                <Button Grid.Column="2"
                                        Content="Browse..."
                                        Style="{StaticResource SecondaryBtn}"
                                        Command="{Binding Replay.BrowseCaptureFileCommand}"
                                        Padding="8,4"/>
                            </Grid>

                            <Separator Margin="0,12,0,10" Background="{StaticResource BorderBrush}"/>

                            <TextBlock Text="gfxrecon-replay.exe"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource DimTextBrush}"
                                       Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding Replay.ReplayExePath, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalContentAlignment="Center"
                                         Height="26"/>
                                <Button Grid.Column="2"
                                        Content="Browse..."
                                        Style="{StaticResource SecondaryBtn}"
                                        Command="{Binding Replay.BrowseReplayExeCommand}"
                                        Padding="8,4"/>
                            </Grid>

                            <Separator Margin="0,12,0,10" Background="{StaticResource BorderBrush}"/>

                            <TextBlock Text="Extra arguments  (optional)"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource DimTextBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Replay.ExtraArgs, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalContentAlignment="Center"
                                     Height="26"/>

                        </StackPanel>
                    </GroupBox>

                    <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                                  Background="{StaticResource BorderBrush}"/>

                    <!-- Right: status + replay button -->
                    <GroupBox Grid.Column="2" Header="Replay">
                        <StackPanel>

                            <Border CornerRadius="4" Padding="10,14" Margin="0,0,0,12">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A2A1A"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Replay.IsReplaying}" Value="True">
                                                <Setter Property="Background" Value="#0F1A28"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Replay.StatusMessage}"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource DimTextBrush}"/>
                            </Border>

                            <ProgressBar IsIndeterminate="True" Height="3"
                                         Foreground="{StaticResource AccentBrush}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Margin="0,0,0,12"
                                         Visibility="{Binding Replay.IsReplaying, Converter={StaticResource BoolToVis}}"/>

                            <Button Content="Start Replay"
                                    Style="{StaticResource PrimaryBtn}"
                                    Command="{Binding Replay.StartReplayCommand}"
                                    IsEnabled="{Binding Replay.IsReplaying, Converter={StaticResource InverseBool}, FallbackValue=True}"
                                    FontSize="13" FontWeight="SemiBold"
                                    Padding="0,12"
                                    HorizontalAlignment="Stretch"/>

                        </StackPanel>
                    </GroupBox>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- ── Launch bar ─────────────────────────────────────────────── -->
        <Border Grid.Row="2"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,1"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource DimTextBrush}" FontSize="11"
                               Text="Selected game:"/>
                    <TextBlock FontSize="14" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                               Text="{Binding SelectedGame.Name, FallbackValue='— none selected —'}"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Copy Log"
                        Style="{StaticResource SecondaryBtn}"
                        Command="{Binding CopyLogCommand}"
                        FontSize="12"
                        Padding="14,12"
                        VerticalAlignment="Center"
                        ToolTip="Copy the full session log to the clipboard"/>

                <!-- Only shown while DLLs are staged in the game folder -->
                <Button Grid.Column="3"
                        Content="Remove DLLs"
                        Command="{Binding RemoveStagedDllsCommand}"
                        FontSize="12" FontWeight="SemiBold"
                        Padding="14,12"
                        VerticalAlignment="Center"
                        Foreground="White"
                        ToolTip="{Binding StagedInDir, StringFormat='Remove GFXR DLLs from: {0}'}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background"      Value="#7B2C2C"/>
                            <Setter Property="BorderBrush"     Value="#C0392B"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Cursor"          Value="Hand"/>
                            <Setter Property="Visibility"      Value="Collapsed"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E74C3C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasStagedDlls}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="5"
                        Content="Launch Capture"
                        Style="{StaticResource PrimaryBtn}"
                        Command="{Binding LaunchCaptureCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        FontSize="14" FontWeight="SemiBold"
                        Padding="28,12"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ── Status bar ─────────────────────────────────────────────── -->
        <Border Grid.Row="3" Background="{StaticResource AccentBrush}" Padding="10,0">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="White" FontSize="11"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"/>
        </Border>
    </Grid>
</Window>
